function myFunction() {
  // Get the text field
  var copyText = document.getElementById("myInput");

  // Select the text field
  copyText.select();
  copyText.setSelectionRange(0, 99999); // For mobile devices

   // Copy the text inside the text field
  navigator.clipboard.writeText(copyText.value);

} 

jQuery(function(){
  jQuery(".ouvrir-partage").click(function(){
            jQuery(".pop-up-partage").removeClass('hide');
    });
});

jQuery(function(){
  jQuery(".div-fermer-pop-up span").click(function(){
            jQuery(".pop-up-partage").addClass('hide');
    });
});



jQuery(function(){
  $(".bloc-article-div").mouseover(function() {
    var r = Math.floor(Math.random() * 255);
    var g = Math.floor(Math.random() * 255);
    var b = Math.floor(Math.random() * 255);
    var color = "rgb(" + r + "," + g + "," + b + ")";
    $(this).css("background-color", color);
  });
});

const shareButton = document.querySelector('.new-share');


shareButton.addEventListener('click', event => {
  if (navigator.share) { 
   navigator.share({
      title: 'CGT FAPT',
      url: window.location.href
    }).then(() => {
      console.log('Thanks for sharing!');
    })
    .catch(console.error);
    } else {

    }
});
